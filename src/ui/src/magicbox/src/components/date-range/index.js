/**
 * @file date-range entry
 * @author ielgnaw <wuji0223@gmail.com>
 */

import bkDateRange from './date-range'

bkDateRange.install = Vue => {
    Vue.component(bkDateRange.name, bkDateRange)
}

export default bkDateRange
